export default function DashboardPage() {
  // TODO: fetch summary from GET /dashboard/summary
  // TODO: connect WebSocket for realtime updates
  return (
    <main>
      <h1>Dashboard</h1>
      {/* TODO: indicators — pending WOs, active attendance, today completions */}
    </main>
  );
}
